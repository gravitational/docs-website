export type Frontmatter ={
  [key: string]: unknown;
}

export type KeyMap = {
    [key: string]: 
}

export const validateFrontmatter = (frontmatter: Frontmatter, allowedFields: Array<string>) => {};
